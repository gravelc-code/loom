# Swift Package Manager build products
.build/
.swiftpm/
Package.resolved
*.xcodeproj
DerivedData/

# Local build scratch (kept outside Dropbox for codesigning)
.cache/

# Compiled app bundle + release zip — distributed via GitHub Releases, not committed
loom.app/
loom-*.zip

# macOS
.DS_Store
*.xcuserstate

# Editor
*.swp
.idea/

# AI/agent tooling — never publish these
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
MEMORY.md
.claude/
.codex/
.cursor/
.cursorrules
.aider*
.github/copilot-instructions.md
.mcp.json
